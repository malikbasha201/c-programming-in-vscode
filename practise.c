#include<stdio.h>
#include<math.h>

int main(){
  int length,breadth;
  printf("The length of the rectangle is :");
  scanf("%d",&length);
  printf("The breadth of the rectangle is:");
  scanf("%d",&breadth);
  printf("Area is :%d\n",length*breadth);
  printf("%f\n",pow(2,5));
  return 0;
}